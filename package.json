{
  "name": "phonetic",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "build": "tsc",
    "test": "mocha --require ts-node/register '**/*.test.ts'",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "audit": "npm audit",
    "type-check": "tsc --noEmit",
    "check": "npm run type-check && npm run lint && npm run audit",
    "test-all": "npm run test && npm run lint && npm run audit && npm run type-check"
  },
  "keywords": ["tenderboard", "case-study", "quiz"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "chai": "^4.3.7",
    "eslint": "^8.0.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
